[project]
name = "screentime-shared"
version = "0.1.0"
description = "Shared data models for Screen Time Tracker"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
